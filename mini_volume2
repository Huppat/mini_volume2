#!/bin/bash
# Mini-Volume2 Tray App - Executable Script
# Lautstärke-Kontrolle mit Mausrad in der Taskleiste

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
python3 "$SCRIPT_DIR/mini_volume2_xdot.py" "$@"
